@model EDL4.Models.Pais

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Pais</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre)
        </dd>

    </dl>
</div>
<h3>cambios</h3>

    @if (Model.cambios != null)
{
    foreach (var item in Model.cambios)
    {
        <tr>
          
                @Html.DisplayFor(modelItem => item) |@Html.ActionLink("Delete", "Deleteca", new { id = item, pais = Model.nombre })
            

                
          
        </tr>
        <br>

        }

    }

<h3>coleccionadas</h3>

    @if (Model.coleccionadas != null)
    {
        foreach (var item in Model.coleccionadas)
        {
            <tr>

                @Html.DisplayFor(modelItem => item)

            </tr>
            <br>

        }

    }

<h3>faltantes</h3>

    @if (Model.faltantes != null)
    {
        foreach (var item in Model.faltantes)
        {
            <tr>

                @Html.DisplayFor(modelItem => item) |@Html.ActionLink("Delete", "Deletefa", new {id=item, pais = Model.nombre }) 

            </tr>
            <br>

        }

    }


<p>
   
    @Html.ActionLink("Back to List", "Index")
</p>
